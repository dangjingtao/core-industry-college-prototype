# T050｜校园大使 V1.1：展示名统一与团队名模型

> 类型：共享模型 / 展示语义 / 施工  
> 状态：PASS  
> 日期：2026-08-27  
> 前置：T049  
> 来源：`docs/product/18-campus-ambassador-prd.md` V1.1

---

## 1. 目标

为 V1.1 提供统一地基：完成校园大使计划所有用户可见 / 运营可见身份名称调整，并给团队增加后台可识别的团队名。

本卡只处理共享语义和团队名，不提前施工 PC 周运营、问卷答案详情或 App 经营看板。

## 2. 展示名统一

所有产品展示统一使用：

- `核心大使` → **校园大使**；
- `推广伙伴` → **校园推荐官**。

覆盖范围至少包括：

- Mobile 页面标题、身份标签、状态说明、按钮、扫码文案、空态 / 错误提示；
- PC 活动列表、团队列表、团队详情、申请信息、成员身份、运营说明；
- 二维码卡片 / Modal / 下载文件名中的用户可见语义；
- 活动条款正式正文；
- 导出文件中的身份展示字段；
- 新增测试用例中面向产品的断言文案。

### 不改

- `AmbassadorMemberRole = "ambassador" | "partner"`；
- 既有类型、函数、文件、路由中的技术命名；
- 活动名称「核心大使计划」。

历史任务卡 T043–T049 作为当时实现记录，不要求批量改写标题和历史证据。

## 3. 团队名模型

为 `AmbassadorTeam` 增加稳定团队名字段。

规则：

1. 后台可指定 / 修改团队名；
2. 未指定时系统自动生成**可读且在本期活动中可区分**的兜底名称；
3. 团队名属于团队本身，与该团队的**团队招募码**一一对应；
4. 不把团队名存进二维码 code 文本本身，二维码仍解析到 team；
5. 首版团队名只用于后台识别、周运营分析、导出等运营场景；
6. **App 暂不展示团队名**；
7. 旧 seed / 已有团队无 teamName 时必须兼容自动兜底，不能因 schema 升级失效。

建议兜底语义使用学校 / 校园大使可读信息 + 短序号，不向业务页面暴露 accountId 或长技术 ID。

## 4. 条款同步

现有已发布 V1.0 原型条款中涉及「核心大使 / 推广伙伴」的展示正文需要同步为最新身份名称。

技术 ID 可继续沿用，不要求因文案调整迁移历史引用。

## 5. 验收

- [x] PC / Mobile 正常业务页面不再展示旧身份名「核心大使 / 推广伙伴」；
- [x] 活动名「核心大使计划」保持不变；
- [x] 底层 `ambassador / partner` role 与既有技术命名不迁移；
- [x] `AmbassadorTeam` 支持 teamName；
- [x] 后台可为团队设置团队名；
- [x] 未设置团队名时有稳定、可读的兜底名称；
- [x] 团队名与团队招募码解析到同一 team，不引入第四种二维码；
- [x] App 不展示团队名；
- [x] 旧 seed / 旧团队数据兼容；
- [x] 活动条款用户可见身份名称同步；
- [x] typecheck / build 通过，并补必要语义回归。

## 6. 明确不做

- 不改底层角色枚举；
- 不重命名历史 T043–T049 文件；
- 不改变三类二维码业务边界；
- 不在本卡做周运营统计或导出；
- 不在本卡改造 App 团队成果页面结构。

## 7. 施工记录

- 共享 `AmbassadorTeam` 增加兼容 V1 旧持久化数据的 `teamName`，读取 seed、localStorage 与 PC / Mobile bridge 状态时统一规范化；
- 未设置团队名时通过 `ambassadorTeamDisplayName()` 生成稳定短名称，不向团队主识别暴露 accountId / 长技术 ID；
- 新建团队立即获得稳定 teamName，后台修改 / 清空团队名通过共享 `setAmbassadorTeamName()` mutation 落到同一状态源；
- teamName 只挂在 team 上，团队招募码继续通过 `teamId` 解析，二维码 code 文本不包含 teamName；
- PC 团队列表 / 团队详情改为以 teamName 作为主识别，并提供“后台团队名”编辑入口；
- Mobile 申请、入队、团队、成果、推广、我的页与扫码器统一使用「校园大使 / 校园推荐官」，且不消费 / 展示 teamName；
- 活动条款正文同步新身份称谓，活动品牌名「核心大使计划」保持不变；
- T044 / T045 / T048 / T049 既有产品断言同步新展示语义；新增 Mobile `t050-ambassador-display-names.spec.ts` 与 PC `t050-ambassador-team-name.spec.ts`；
- Prototype Quality Gate 已纳入 Mobile T050 语义回归；PC Gate 原本执行整套 PC E2E，因此自动覆盖 PC T050。

## 8. PASS 证据

- Prototype Quality Gate：run `33053692806`，head `4047eb9b27f0ff49f5ce74c6d4068b183fbe334b`，结论 `success`；
- Mobile routes / typecheck / production build：success；
- Mobile browser regressions（含 T044 / T045 / T048 / T049 / T050）：success；
- real Mobile-PC-Mobile handoff：success；
- PC typecheck / production build：success；
- PC browser regressions（整套 E2E，含 T050 teamName 编辑 / 旧状态兼容）：success；
- Mobile T050 验证新身份称谓、活动名保留、App 不展示后台 teamName、条款正文同步；
- PC T050 验证后台改名持久化、团队招募 code 不变，以及旧状态缺失 teamName 时生成稳定可读兜底并回写。

## 9. 完成结论

T050 已在 `dev` 完成并通过工程回归。V1.1 后续 T051–T054 可以在统一身份语义和稳定 teamName 地基上继续施工。

**结论：PASS。**
