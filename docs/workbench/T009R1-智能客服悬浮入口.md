# T009R1｜智能客服入口改为右下角悬浮蓝色窗口与交互优化

**类型：施工卡（T009 方向修订）**  
**状态：已完成**  
**优先级：P1**

## 当前原型完成度

- [x] 全站右下角蓝色悬浮客服入口已常驻于 `PublicShell`，所有使用 `PublicShell` 的页面均可访问。
- [x] `/support/chat` 客服对话页已优化：
  - 欢迎语贴合当前产品（赛事、实习、课程、权益、可信空间、投递）。
  - 热门问题支持点击直接发送。
  - 消息气泡、输入框、发送按钮视觉与动线优化（自动滚动、typing 指示器、答案反馈）。
  - 无知识库答案时提示「已记录，后续可接入大模型/转人工客服」，连续 2 次未命中自动提示转人工。
  - 右上角/底部人工客服入口，点击弹出企业微信二维码占位弹窗。
- [x] `/support` 帮助中心重构：
  - 搜索框移除，改为「常见问题」+ 分类 tab（热门 / 报名 / 赛事 / 权益 / 课程 / 账号）。
  - 热门问题直接展示答案，无需展开。
  - 分类问题直接展示问答卡片。
- [x] 路由 `/support` 与 `/support/chat` 保持与 T009 一致，未破坏 URL 一致性。
- [x] 通过 mobile `typecheck` / `build`。

## 设计文档链接

- 父卡：`docs/workbench/T009-智能客服.md`
- 工作台账：`docs/workbench/00-work-ledger.md`
- 相关实现文件：
  - `apps/mobile/src/components/ui.tsx`（`PublicShell` 全局悬浮入口）
  - `apps/mobile/src/features/platform-support/SupportPages.tsx`（帮助中心 + 智能客服对话页）
  - `apps/mobile/src/features/public-platform/PublicPlatform.tsx`（移除首页局部入口）

## 背景

T009 已实现智能客服对话页，并在首页提供悬浮圆形入口。

昨天下班前内部会议要求：

1. 客服入口改为页面右下角悬浮的蓝色客服窗口，确保用户能便捷找到；
2. 客服基于知识库回复；对于无答案的问题，后续可接入大模型；
3. 优化客服界面的交互细节。

## 目标

将客服入口从首页局部按钮升级为全站右下角悬浮蓝色窗口，优化对话页交互，并保持中保真可演示。

## 最低交付

- [ ] 全站右下角悬浮蓝色客服入口按钮（不仅限于首页），常驻于 `PublicShell` 或全局布局中。
- [ ] 入口视觉：蓝色主色调、悬浮胶囊/圆形窗口，不遮挡底部导航关键操作。
- [ ] 点击入口后进入 `/support/chat` 客服对话页（或展开浮窗对话层）。
- [ ] 客服对话页交互优化：
  - 欢迎语更贴合当前产品（赛事、实习、课程、权益）。
  - 热门问题按场景分组（报名 / 赛事 / 课程 / 证书 / 实习机会 / 账号 / 人工）。
  - 消息气泡、输入框、发送按钮视觉与动线优化。
  - 无知识库答案时，提示「已记录，后续可接入大模型/转人工客服」。
- [ ] 保留转人工客服入口（企业微信二维码占位），不伪造真实在线状态。
- [ ] 通过 mobile `typecheck` / `build`。

## 禁止

- 不伪造真实人工客服在线状态。
- 不构造无法解释答案来源的 AI 回复。
- 不因为入口全局化就破坏现有路由与 URL 一致性。

## 验收动线

```text
任意页面 → 右下角悬浮蓝色客服入口
  → 进入 /support/chat
  → 欢迎语 + 场景化热门问题
  → 点击问题 / 手动输入 → 知识库匹配回复
  → 无答案 → 提示接入大模型或转人工
  → 右上角/底部 → 请求人工客服 → 企微二维码弹窗
```

## 依赖

- T009｜智能客服原型（已完成）
