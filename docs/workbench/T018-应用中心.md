# T018｜应用中心（tabbar 第 5 入口 + 分组宫格聚合页）

**类型：施工卡**  
**状态：待执行**
**优先级：P1**

## 背景

当前手机端 tabbar 为 4 项：

```text
首页 /home │ 赛事 /competitions │ 机会 /opportunities │ 我的 /me
```

平台支撑型能力（课程、权益、可信空间、任务中心、赛友内容、快速验真、公告、客服等）入口分散：

- 首页有「成长资源」5 宫格 + 任务专区；
- 「我的」页有「服务入口」列表；
- 部分功能藏在二级 / 三级页面（兑换中心、我的卡券、成长概览、快速验真、投递记录、我的比赛团队、公告资讯），用户需要时难以快速找回。

产品总纲（`docs/product/00-product-master-context.md`）已明确：*"课程很重要""可信空间很重要"都不自动等于它们必须占底部一级导航*，且不要大量能力平铺。支撑能力应增强「参赛 / 就业」两条主线，而不是抢一级导航主叙事——但它们仍需要一个稳定的全量出口。

## 产品定义（2026-08-19 与需求方确认）

> **应用中心 = 平台「功能工具」的全量出口。** 凡是不属于参赛 / 就业主线、但用户需要时应能翻到的功能，都收进这里，用分组宫格承载。它不做成「小程序 / 应用商店」式市场，而是类似支付宝服务页的分组功能入口。

设计原则：

1. **定位明确**：主线（参赛 / 就业）继续占一级导航；应用中心只收工具性、内容性、福利性功能。
2. **真实跳转**：每个入口都跳现有真实路由，不造假按钮、不新建第二套页面。
3. **全量聚合，不改现状**：首页 5 宫格、我的页服务入口原样保留；应用中心做全量集，允许短期存在重复入口，后续可再清理（本卡不做入口去重改造）。
4. **扩展性好**：分组结构便于后续新增模块（如创域 / 线下运营解冻后）直接加入。

## 目标

- tabbar 从 4 项扩展为 5 项：首页 / 赛事 / **应用中心** / 机会 / 我的；
- 新增 `/apps` 分组宫格页，作为支撑功能的统一快速入口；
- 把目前藏得深的入口（兑换中心、我的卡券、成长概览、快速验真、投递记录、我的比赛团队、公告资讯等）显性化。

## 分组与入口清单（基于现有 66 条语义路由盘点）

### 学习成长
| 入口 | 路由 | 说明 |
| --- | --- | --- |
| 课程学习 | `/courses` | 公共课程 |
| 学力值 | `/growth/score` | 学力值余额与明细（原"成长概览"，2026-08-19 主题替换为学力值，占位口径 1280） |
| 可信空间 | `/assets` | 证书 / 成绩 / 经历总览 |

### 福利权益
| 入口 | 路由 | 说明 |
| --- | --- | --- |
| 创赛福利 | `/benefits` | 福利首页 |
| 我的卡券 | `/benefits/wallet` | 藏在三级，需快速找回 |
| 兑换中心 | `/benefits/exchange` | 学力值兑换，藏得深 |

### 工具与服务
| 入口 | 路由 | 说明 |
| --- | --- | --- |
| 创赛工坊 | 动态 → `/competitions/:competitionId/workspace/workshop` | 赛事 AI 陪跑；有 active 赛事身份时直达对应赛事工坊，无身份时引导去赛事发现 |
| 任务中心 | `/tasks` | 已有业务下一步聚合页 |
| 投递记录 | `/applications` | 只在机会页 tab 内 |
| 快速验真 | `/assets/verification` | 需要独立曝光 |
| 公告资讯 | `/news` | 藏得深 |
| 三创同学会 | `/stories` | 赛友内容 |
| 合作企业 | `/companies` | 企业资源 |
| 帮助与客服 | `/support` | 兜底服务 |

### 消息与我的
| 入口 | 路由 | 说明 |
| --- | --- | --- |
| 我的比赛团队 | `/me/teams` | 藏得深 |
| 消息通知 | `/me/notifications` | 快速入口 |

**不进应用中心**：投稿（低频内容生产者动线）、账号绑定 / 设置 / 协议（「我的」页天生职责）、赛事 / 机会（主线一级导航）。

> 创赛工坊特殊说明：它不是全局 AI 工具箱，而是具体赛事上下文能力。应用中心作为公共全量出口，工坊入口按"有 active 赛事身份时直达对应赛事工坊、无身份时引导去赛事发现"实现，保留具体 `competitionId`，不造全局工坊对象（与首页任务专区 / `/tasks` 的既有定位逻辑一致）。

## 最低交付

- [x] tabbar `navItems` 增加「应用中心」，`to: /apps`，图标 `LayoutGrid`；位置在「赛事」与「机会」之间；5 项 `flex-1` 平分，无布局溢出。
- [x] 新增 `features/app-center/AppCenterPage.tsx`：分组宫格页，复用 `PublicShell` / `Section` / `Card` / `lucide-react` 图标，与首页「成长资源」宫格视觉语言一致。
- [x] `routes/registry.ts` 增加 `appCenter.home` 定义；`App.tsx` 注册 `/apps` 路由（保留 SPA fallback，深层刷新不 404）。
- [x] 所有入口为真实路由跳转，覆盖 16 项分组入口（含动态创赛工坊）；不存在死链或假按钮。
- [x] 未登录状态下应用中心可正常浏览；需要登录的入口按现有 `AccountRequired` / 引导登录逻辑承接（`/growth/score`、`/assets`、`/benefits/wallet`、`/applications`、`/me/teams`、`/me/notifications` 等由各目标页自身守卫）。
- [x] 同步更新 route audit / 迁移文档中的路由清单。

## 禁止

- 不把应用中心设计成「小程序 / 应用商店」形态（无第三方应用、无安装 / 卸载语义）。
- 不为应用中心新建任何业务真相源（无新 store、无新状态模型）；页面只做入口聚合跳转。
- 不修改首页「成长资源」宫格与「我的」服务入口（本卡不做去重改造）。
- 不把赛事 / 机会等主线入口迁入应用中心。
- 不为应用中心恢复 D03 旧任务列表、D08 主体管理等冻结功能。

## 验收动线

```text
任意 tab 进入
→ 应用中心（/apps）
→ 分组宫格浏览
→ 点击任一入口 → 到达对应真实页面 → 返回应用中心

深度链接：直接访问 /apps 刷新不 404
未登录：/apps 可浏览，需登录入口引导登录 returnTo /apps
```

## 依赖

- 无（可与其它 T / F 卡并行）
- 路由注册需保留 SPA fallback 约定

## 施工记录

- 开始时 branch HEAD：`c652588`（建卡后）；实施时已在 `dev` 上，未与并行改动冲突。
- 实际修改范围：
  - `apps/mobile/src/components/ui.tsx`（tabbar navItems + LayoutGrid）
  - `apps/mobile/src/routes/registry.ts`（`appCenter.home` + `candidatePrimaryNavigation`）
  - `apps/mobile/src/features/app-center/AppCenterPage.tsx`（新增，4 组 15 入口宫格）
  - `apps/mobile/src/app/App.tsx`（注册 `/apps`）
  - `apps/mobile/tests/t018-app-center.spec.ts`（新增浏览器用例）
  - `docs/migrations/mobile-from-com-design.md`（route 基线同步）
  - `docs/workbench/T018-应用中心.md`（本卡施工记录）
- 分组实现：学习成长 3 / 福利权益 3 / 工具与服务 8（含创赛工坊）/ 消息与我的 2，共 16 个入口，全部跳现有真实路由；
- 学力值主题替换（2026-08-19 与需求方确认，范围=主题替换占位不变、口径=沿用 1280）：`/growth/score` 由"成长分（基础60+学习*20+投递*10）"改为"学力值"占位主题（余额 1280 + 明细入口复用 `/benefits/credits`），应用中心入口"成长概览"→"学力值"（Coins 图标），保持"待 F04 决策"标注；不自行定义收入/消耗经济规则，不统一福利/兑换/课程其它占位，不改 F04 冻结状态；
- 创赛工坊动态入口：读取 `identities[]` 中 active 赛事身份 → 直达 `/competitions/:competitionId/workspace/workshop`（保留 competitionId，不造全局工坊）；无 active 身份时引导去 `/competitions` 发现赛事；默认场景（`sanchuang-16` active）下指向 `/competitions/sanchuang-16/workspace/workshop`；
- 未登录（退出登录后）`/apps` 可浏览，需登录目标页由各自 `AccountRequired` 守卫承接，应用中心本身不重复建登录态；
- 不新增任何 store / 状态模型，仅做入口聚合；
- 未修改首页「成长资源」宫格与「我的」服务入口（本卡不做去重改造）；
- 2026-08-20：tabbar「机会」与「应用中心」调换位置，顺序调整为 首页 / 赛事 / 应用中心 / 机会 / 我的；同步 `candidatePrimaryNavigation` 与 t018 用例顺序断言；
- 验证证据：
  - route audit：`node scripts/audit-routes.mjs` → Registry 82 / Missing 0 / RouteProbe no / Explicit 404 yes / **Route audit PASS**；
  - build：`npm run build:mobile`（tsc -b + vite build）PASS；
  - 浏览器：`npx playwright test tests/t018-app-center.spec.ts` → **6/6 passed**（分组与入口 href、创赛工坊 active 定位并进入工坊、学力值入口进入占位页、tabbar 5 项顺序、入口点击跳转与返回、`/apps` 深层刷新不 404）。
- 实现提交 SHA：待填写
- 评审提交 SHA：待填写
- 当前结论：实现完成，等待独立功能评审；施工线程不自行标记 `PASS`。