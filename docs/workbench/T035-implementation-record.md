# T035｜兑换中心 / 卡券手机号绑定体验｜原型记录

> 状态：原型已施工  
> 日期：2026-08-24  
> 正式规则：`docs/product/14-exchange-center-and-phone-bound-benefits.md`

## 本轮修改

新增：

- `apps/mobile/src/features/long-term-assets/T035ExchangePages.tsx`

主路由切换：

- `/benefits/exchange`
- `/benefits/exchange/:exchangeId`

旧 `BenefitsPages.tsx` 中的兑换中心实现继续保留作迁移历史，不删除。

## 现有流程

```text
兑换中心
→ 进入详情
→ 查看学力值消耗 / 到账方式
→ 确认消耗
→ 成功结果
```

线上权益在当前原型中使用当前登录账号已验证手机号作为到账手机号，不在兑换页临时输入另一个手机号。

课程兑换直接进入学习账户，不要求手机号。

## 与 T034 对齐

新的课程兑换列表直接读取当前 `creditRequired` 课程：

- OPC 创新创业方法论；
- 乡村振兴电商运营；
- AI 电商智能体实战。

不再在主兑换列表展示已经免费化的旧课程商品。

`AI 电商智能体实战` 继续显示“可信证书课程”标识，但证书签发规则仍由 T034 管理。

## 原型状态

当前处理：

- 学力值不足；
- 已兑完；
- 手机号未绑定；
- 已兑换；
- 旧课程兑换链接兼容。

非课程权益的扣减 / 已兑换状态使用 sessionStorage 作为原型状态，不代表生产订单或发券实现。

## 不做

- 不做订单中心；
- 不做真实库存；
- 不做真实发券 API；
- 不做退款；
- 不做转赠；
- 不决定换绑手机号后的历史卡券迁移；
- 不扩展新的手机号账户模型。

## 施工提交

- `46da7537547365277f68f093b056d92b89cbac82` — T035 兑换确认 / 手机号到账原型
- `07804679cd71bd7f59b032d4d7801f4a31413ac0` — 主路由切换至 T035
